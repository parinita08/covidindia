import React from 'react';

function Table({states}) {
    return (
        <div className="table">
            {
                // states.map(()=> (
                <tr>
                    <td>hello</td>
                    <td>hi</td>
                </tr>
                // ))
            }
        </div>
    )
}

export default Table